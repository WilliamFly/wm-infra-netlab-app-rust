# Copy to .env on the app VM and fill in the real password.
# .env is gitignored — never commit the real one.

DATABASE_URL=postgres://app_user:REPLACE_ME@10.0.3.20:5432/netlab_app
PORT=8080
SERVED_BY=netlab-app-rust
